[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "final-nn"
version = "0.1.0"
description = "Neural network final project"
requires-python = ">=3.10"
dependencies = ["numpy"]

[tool.setuptools]
packages = ["nn"]